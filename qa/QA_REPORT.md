# QA_REPORT.md — 最終品質検証報告書

**対象システム**: Airis 販売代理店支援ポータル
**リポジトリ**: github.com/kotakase2022-jpg/airis（最終検証コミット: 7d8d97d）
**本番環境**: https://airis-nine.vercel.app
**仕様書**: `docs/SPEC.md`（= CODEX_INSTRUCTIONS.md、一次ソース）
**QA体制**: 独立QA責任者兼修正責任者（実装者と同一だが、判定は未関与の独立レビューエージェントによる二重検収を実施）
**QA期間**: 2026-08-05 03:05 〜 10:30 JST

---

## 1. 最終判定

# **条件付き合格**

| 指標 | 値 |
|---|---|
| 要件分解数 | **564件**（仕様書全章を一意の要件IDに分解） |
| 要件合格率 | **483 / 564 = 85.6%** |
| 発注者判断による先送り（スコープ外）を除いた合格率 | **483 / 548 = 88.1%** |
| 自動テスト成功率 | **560 passed / 0 failed**（E2E 331 + 単体 216 + 本番スモーク 13。skipped 3は排他構成分で両構成合算では全実行） |
| 未検証（BLOCKED） | **1件**（SEC-018: インフラ設定エビデンスの提示が必要。実装検証は不能） |
| 残存不具合 | **2件**（major 1 = S3抽象化未実装〔発注者先送りR-01〕、minor 1 = Prettier未導入） |
| 検出・修正した不具合 | **54件検出 / 52件修正**（critical 3件はすべて修正済み） |

### 本番リリース可否: **条件付き可**

**可の根拠**:
- critical/major のセキュリティ・データ破壊系欠陥は **0件**（IDOR、認証基盤、RLS、監査ログ、IP偽装対策まで実機検証済み)
- 全10ロールのログイン〜主要業務フローが本番環境で動作（スモーク13/13）
- 未関与の独立レビューエージェントによる最終検収で「critical 0 / major 0」を確認

**条件（リリース前に発注者の判断・対応が必要)**:
1. **MFA未実装（発注者先送りR-03）**: 仕様§10は「SEC要件準拠がクラウドレビューのリリース条件」と明記しており、パスワード単一要素のままでは**この条件を形式上満たさない**。社外公開・本番データ投入の前に、リスク受容の正式決定または実装が必要
2. **デモパスワードの変更**: `prisma/seed.ts` のパスワードは開発用。本番運用開始前に全アカウントで変更必須
3. **データ所在リージョン（SEC-023）**: Neonが東京リージョンでない場合、国内リージョン要件を満たさない。プロジェクトリージョンの確認・移設が必要
4. **`PASSWORD_PEPPER_V1` の設定**: 現在は未設定で稼働。Vercelダッシュボードから設定を推奨（既存ユーザーはログイン時に自動再ハッシュされ無停止移行）

---

## 2. 検証方法（実施サマリ）

要件564件を「コード読解のみでPASSにしない」原則で検証した。

| 検証手段 | 対象 | 規模 |
|---|---|---|
| E2E（Playwright・実ブラウザ） | 全11画面 × 全10ロール、正常/異常/境界/権限 | 21スイート・331テスト（プロキシ構成含む） |
| 単体（Vitest） | §5.1権限マトリクス全数（9機能×10操作×10ロール）、IP信頼判定、CSV、ユーティリティ | 216テスト |
| DB直接検証 | RLSポリシー（airis_appロールで0件確認）、スキーマ、ハッシュ形式、匿名化結果 | psql/Prisma実測 |
| API直接検証 | 認可（403/401）、IDOR、CSVエクスポート、cron認証 | curl実測 |
| 本番検証 | 全10ロール実ログイン、保護ページリダイレクト、cron認証、テンプレ配信 | 13テスト |
| 静的検証 | プロセス要件・ドキュメント要件・設定値 | 手動照合 |
| 独立レビュー | 未関与エージェントによる敵対的再現検証（2回） | 33指摘 → 全て修正または残存リスク化 |

**QA原則の遵守**: 期待値の弱化・テスト削除・スキップによる回避は行っていない。修正53件すべてでクリーンDBからの全件回帰を再実行した。

---

## 3. 要件合否の内訳

| 判定 | 件数 | 内容 |
|---|---|---|
| PASS | 483 | 実機・実データで期待結果を確認 |
| FAIL | 49 | うち**6件は発注者先送り**（S3等）。残り43件は§8参照 |
| NOT_IMPLEMENTED | 31 | うち**10件は発注者先送り**（MFA/Slack） |
| BLOCKED | 1 | SEC-018（インフラ設定エビデンス要。実装外） |

### 発注者判断によるスコープ外 16件（詳細は RESIDUAL_RISKS.md）
- **R-01 ファイルストレージのS3抽象化**（A-027, D-016ほか）— DB(bytea)格納のまま
- **R-02 Slack通知**（A-029, S8-030ほか）— アプリ内通知+メールで代替
- **R-03 MFA/TOTP**（A-024, A-025, A-048, SEC-002, SEC-003ほか）— Argon2id+ロック+検知で緩和

---

## 4. 未PASS（先送り除く65件）の性質分析

**セキュリティ・データ破壊・業務停止に該当するものは0件。** 内訳:

| 分類 | 件数 | 代表例 | 業務影響 |
|---|---|---|---|
| 開発プロセス要件 | 8 | PR運用なし（A-003, PROC-011/012）、AGENTS.md内容不足（A-004）、Prettier（A-031）、Prisma Migrate未使用（A-021） | 実行時影響なし。保守性・変更管理の課題 |
| シードデータの件数不足 | 9 | 販売員20名→不足（D-028）、日報1ヶ月分→不足（D-030）、案件返信ラリー（D-033） | デモの見栄えのみ。機能検証は完了済み |
| SEC要件のうち文書・運用・インフラ系 | 15 | 構造化ログJSON出力（SEC-030）、検証環境定義（SEC-037）、リージョン（SEC-023）、鍵管理文書 | 実装というより運用整備の課題 |
| 画面仕様の細部差異 | 18 | ヘッダのページアイコン（UI-018）、絵文字排除（UI-25）、ダッシュボード第4カードのラベル（S2-010）、販売員一覧の「未申請」表示（S4-023, R-018） | 表示の細部。データ・権限は正しい |
| 設計方式の仕様差異（機能は同等） | 8 | ④ダミーの物理分離→論理分離（D-021, A-067, D-034）、ステータスマスタ化→定数（S8-020）、スコープ2列→1列+親子解決（A-033） | 多層防御・テストで安全性は担保。将来の変更耐性の課題 |
| 機能の部分未達 | 7 | 返信状況カード（S1-012）、訪販申請の行内ボタン（S4-025）、zipアップロード（S4-029）、履歴の専用テーブル化（R-022） | 該当機能の利便性低下。回避手段あり |

全65件の要件ID・証拠・実装参照は `REQUIREMENTS_TRACEABILITY.csv`（564行）に記録。

---

## 5. 検出した重大欠陥と修正（抜粋）

| ID | 重要度 | 内容 | 状態 |
|---|---|---|---|
| BUG-C01 | critical | **IDOR**: `/files/[id]` が無認可で、全ロールが他代理店の誓約書PDF等を取得可能だった | 修正+E2E全分岐 |
| BUG-C02 | critical | パスワードがbcryptのみ（Argon2id・ペッパー・レート制限なし） | 修正（無停止移行） |
| BUG-C03 | critical | **本番で全アカウントがログイン不能**（seed.tsのハッシュ方式がアプリと不整合） | 修正+本番再シード+README再発防止 |
| BUG-M02 | major | アクセスログ（IP/UA記録）が存在しなかった | 修正（AccessLog新設） |
| BUG-M05 | major | x-vercel-forwarded-for を無条件信頼（IP偽装可能） | 修正（オプトイン+末尾hop+fail-closed） |
| BUG-M09 | major | RLS未適用（アプリ層スコープのみ） | 修正（8テーブル+airis_appロール） |

全54件は `BUG_REPORT.md` 参照。

---

## 6. テスト実行ログ（最終回帰・2026-08-05）

```
E2E 既定構成:    322 passed / 2 skipped / 0 failed  (3.3m)   ← ペッパー有効構成で実行
E2E プロキシ構成:  9 passed / 1 skipped / 0 failed
単体 (Vitest):   216 passed / 0 failed
本番スモーク:     13 passed / 0 failed  (14.4s)  ← 全10ロール実ログイン
lint / tsc / build: 0 エラー
```

skipped 3件は排他な起動構成の相互分（既定⇔プロキシ）で、両構成合算で全テスト実行済み。

---

## 7. 成果物一覧

| ファイル | 内容 |
|---|---|
| `qa/QA_REPORT.md` | 本書（最終判定） |
| `qa/REQUIREMENTS_TRACEABILITY.csv` | 要件564件の完全トレーサビリティ（要件ID→テストID→証拠→判定） |
| `qa/TEST_CASES.md` | テストケース532件（正常/異常/境界/権限） |
| `qa/BUG_REPORT.md` | 不具合54件の報告書（再現手順・原因・修正・検証） |
| `qa/RESIDUAL_RISKS.md` | 残存リスク（発注者先送り3件+QA検出7件+BLOCKED一覧） |
| `airis/e2e/*.spec.ts` | E2Eテストコード21スイート |
| `airis/tests/unit/*.test.ts` | 単体テストコード |
| `airis/e2e-prod/prod-smoke.spec.ts` | 本番スモークテスト |

**変更ファイル**: QA期間中の修正はリポジトリのコミット履歴（042b756〜7d8d97d）に記録。主要新規: `src/lib/file-access.ts` / `src/lib/client-ip.ts` / `prisma/rls.sql` / `src/lib/permissions.ts`。

---

## 8. 独立レビューの結論（未関与エージェント）

- 第1回（修正前）: **不合格**（critical 2 / major 8 / minor 8）
- 第2回（修正後・最終）: **条件付き合格**（critical 0 / major 0 / minor 10 — うち9件修正済み、1件=Prettierは残存リスク化）
- 第6次修正後の再判定（本報告の数値）: 未PASS93件を実機で再検証し、12件をPASSへ、残81件を FAIL/NOT_IMPLEMENTED/BLOCKED として確定

**判定の独立性**: 最終判定に使用した93件の再検証は、修正作業に関与していないエージェントが実機（ブラウザ/API/DB/git履歴）で証拠を取得して実施した。
